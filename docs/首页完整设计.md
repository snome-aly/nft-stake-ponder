# 盲盒 NFT 项目首页完整设计

**项目名称**: StakableNFT 盲盒系统
**页面**: 首页 (/)
**设计版本**: v2.0 (Contract-Accurate)
**最后更新**: 2025-01-27

---

## 📋 目录

- [1. 项目核心参数](#1-项目核心参数)
- [2. 整体布局结构](#2-整体布局结构)
- [3. Header 导航栏设计](#3-header-导航栏设计)
- [4. 主要内容区域](#4-主要内容区域)
- [5. Footer 底部设计](#5-footer-底部设计)
- [6. 响应式设计](#6-响应式设计)
- [7. 交互效果](#7-交互效果)
- [8. 技术实现](#8-技术实现)

---

## 1. 项目核心参数

### 1.1 合约常量 (StakableNFT.sol)

```solidity
总供应量 (MAX_SUPPLY):          100 NFTs
Mint 价格 (MINT_PRICE):         1 ETH
每地址限额 (MAX_PER_ADDRESS):    20 NFTs
最大奖励倍率 (MAX_MULTIPLIER):   100000 (10x)
```

### 1.2 稀有度分布

| 稀有度 | 数量 | 占比 | 奖励倍率 | 倍率值 (基数10000) |
|--------|------|------|----------|--------------------|
| Common | 50 | 50% | 1.0x | 10000 |
| Rare | 30 | 30% | 1.5x | 15000 |
| Epic | 15 | 15% | 2.0x | 20000 |
| Legendary | 5 | 5% | 3.0x | 30000 |

### 1.3 IPFS 资源

```
盲盒图片: ipfs://Qmd2SzbuXHQnc5jcL7c2ohTpNsKcU2NJVwcPr2bG7S1cKk
Common:   ipfs://QmY7A1WYkzBxgvYXwDwbY35bntWEeYi6kmph52UcpQTHFp
Rare:     ipfs://QmYbvQvfFrKbxLwr3ZX4pigAZJbTB7zCpykGmuoWoZTf9p
Epic:     ipfs://Qmdg4TcyiPpuxUJpDTsnJSGfEsjLzASekNrtCyWQZqWDW6
Legendary: ipfs://QmZHdmbPR711ujJWi1UL6te2H5QsicuvPPuUcbJNnesjtf
```

### 1.4 关键机制

- **盲盒系统**: 所有NFT铸造时为盲盒状态,看不到稀有度
- **批量揭示**: 只有当全部100个NFT售罄后,管理员才能触发揭示
- **VRF随机**: 使用随机偏移量公平分配稀有度(生产环境将使用Chainlink VRF)
- **链上元数据**: 所有metadata由合约动态生成,无需用户提供URI
- **分离式质押**: NFT合约仅存储倍率,实际质押由独立的NFTStakingPool合约处理

---

## 2. 整体布局结构

```
┌─────────────────────────────────────────────┐
│  Header (固定顶部)                           │
│  Logo | 导航菜单 | 连接钱包                   │
├─────────────────────────────────────────────┤
│  Hero Section (英雄区)                       │
│  主标题 + 副标题 + CTA按钮 + 铸造进度         │
│  + 揭示状态指示器                            │
├─────────────────────────────────────────────┤
│  Blind Box Showcase (盲盒机制说明)           │
│  盲盒状态 vs 揭示状态对比 + 揭示流程          │
├─────────────────────────────────────────────┤
│  Rarity Showcase (稀有度展示)                │
│  4种稀有度卡片 + 分布概率 + 奖励倍率          │
├─────────────────────────────────────────────┤
│  Live Activity (实时铸造动态)                │
│  最新铸造记录 + 进度统计                      │
├─────────────────────────────────────────────┤
│  Reveal Process (揭示流程说明)               │
│  6步完整用户旅程 + VRF公平性说明              │
├─────────────────────────────────────────────┤
│  Staking Architecture (质押架构)             │
│  NFT合约 → 质押池合约 → 奖励代币 流程图       │
├─────────────────────────────────────────────┤
│  Stats Dashboard (数据仪表板)                │
│  已铸造 | 揭示状态 | 稀有度统计               │
├─────────────────────────────────────────────┤
│  Footer (页脚)                               │
│  项目信息 | 快速链接 | 社交媒体 | 版权信息       │
└─────────────────────────────────────────────┘
```

---

## 3. Header 导航栏设计

### 3.1 设计要求

- **固定位置**: 使用 `fixed` 定位在页面顶部
- **半透明背景**: `bg-gray-900/80` + 毛玻璃效果 `backdrop-blur-md`
- **响应式**: 桌面端水平导航,移动端汉堡菜单
- **钱包集成**: RainbowKit CustomConnectButton组件
- **简化设计**: 移除冗余的网络选择器(RainbowKit已内置)

### 3.2 Header 组件结构

**已实现**: 参考 `packages/nextjs/components/Header/Header.tsx`

```tsx
// components/Header/Header.tsx
export const Header = () => {
  const pathname = usePathname();
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <header className="fixed top-0 left-0 right-0 z-50 bg-gray-900/80 backdrop-blur-md border-b border-white/10">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="flex items-center h-16 gap-4 sm:gap-8">

          {/* Logo 区域 - 靠左 */}
          <div className="flex-shrink-0">
            <Logo />
          </div>

          {/* 桌面端导航菜单 */}
          <nav className="hidden md:flex items-center space-x-2">
            <NavLink href="/" active={pathname === "/"} icon={<HomeIcon />}>
              Home
            </NavLink>
            <NavLink href="/mint" active={pathname === "/mint"} icon={<CubeTransparentIcon />}>
              Mint
            </NavLink>
            <NavLink href="/my-nfts" active={pathname === "/my-nfts"} icon={<PhotoIcon />}>
              My NFTs
            </NavLink>
            <NavLink href="/stake" active={pathname === "/stake"} icon={<LockClosedIcon />}>
              Stake
            </NavLink>
            <NavLink href="/stats" active={pathname === "/stats"} icon={<ChartBarIcon />}>
              Stats
            </NavLink>
          </nav>

          {/* 右侧钱包连接按钮 */}
          <div className="flex items-center space-x-4 flex-shrink-0 ml-auto">
            <div className="hidden sm:block">
              <CustomConnectButton />
            </div>

            {/* 移动端菜单按钮 */}
            <button
              onClick={() => setIsMenuOpen(!isMenuOpen)}
              className="md:hidden text-white p-2 hover:bg-white/10 rounded-lg"
            >
              {isMenuOpen ? <XMarkIcon className="w-6 h-6" /> : <Bars3Icon className="w-6 h-6" />}
            </button>
          </div>
        </div>

        {/* 移动端导航菜单 */}
        {isMenuOpen && (
          <div className="md:hidden py-4">
            <nav className="flex flex-col space-y-2 px-2">
              {navLinks.map(link => (
                <MobileNavLink key={link.href} {...link} active={pathname === link.href} />
              ))}
              <div className="pt-4 mt-2 border-t border-white/10">
                <CustomConnectButton />
              </div>
            </nav>
          </div>
        )}
      </div>
    </header>
  );
};
```

---

## 4. 主要内容区域

### 4.1 Hero Section (英雄区)

```tsx
// components/sections/HeroSection.tsx
export function HeroSection() {
  const { address } = useAccount();
  const router = useRouter();

  // 读取关键状态
  const { data: totalMinted } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "totalMinted",
  });

  const { data: isRevealed } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "isRevealed",
  });

  const { data: rarityPoolSet } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "rarityPoolSet",
  });

  const MAX_SUPPLY = 100;
  const MINT_PRICE = "1 ETH";
  const progress = totalMinted ? Number((totalMinted * 100n) / BigInt(MAX_SUPPLY)) : 0;

  return (
    <section className="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-purple-900 via-blue-900 to-indigo-900">

      {/* 背景动画粒子 */}
      <div className="absolute inset-0">
        <ParticleBackground />
      </div>

      {/* 浮动装饰元素 */}
      <div className="absolute top-20 left-10 w-20 h-20 text-purple-400/20 animate-float-slow">
        🎁
      </div>
      <div className="absolute bottom-20 right-10 w-16 h-16 text-pink-400/20 animate-float-slow" style={{ animationDelay: '2s' }}>
        💎
      </div>
      <div className="absolute top-40 right-20 w-24 h-24 text-blue-400/20 animate-float-slow" style={{ animationDelay: '4s' }}>
        🌟
      </div>

      <div className="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div className="text-center max-w-5xl mx-auto">

          {/* 主标题 */}
          <div className="mb-8">
            <h1 className="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-bold mb-6">
              <span className="block bg-gradient-to-r from-yellow-400 via-pink-500 to-purple-600 bg-clip-text text-transparent animate-gradient">
                🎁 Mystical
              </span>
              <span className="block bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                Blind Box NFT
              </span>
            </h1>

            <p className="text-xl sm:text-2xl md:text-3xl text-gray-300 max-w-4xl mx-auto leading-relaxed">
              Mint a mysterious blind box and await the grand reveal!
              <br className="hidden sm:block" />
              <span className="text-purple-400 font-semibold">Limited to 100 NFTs</span> — only unlocked after sellout
            </p>
          </div>

          {/* 揭示状态指示器 */}
          {!rarityPoolSet && (
            <div className="mb-8 p-4 bg-yellow-500/20 border border-yellow-500/50 rounded-xl">
              <p className="text-yellow-300 font-semibold">
                ⚠️ Rarity Pool Setup Required — Minting Not Yet Active
              </p>
            </div>
          )}

          {totalMinted === BigInt(MAX_SUPPLY) && !isRevealed && (
            <div className="mb-8 p-6 bg-purple-500/20 border-2 border-purple-500/50 rounded-xl animate-pulse">
              <p className="text-purple-300 font-bold text-2xl">
                🎉 SOLD OUT! Awaiting Admin Reveal...
              </p>
              <p className="text-gray-300 mt-2">
                All 100 NFTs minted. Rarities will be revealed soon via VRF randomness!
              </p>
            </div>
          )}

          {isRevealed && (
            <div className="mb-8 p-6 bg-green-500/20 border-2 border-green-500/50 rounded-xl">
              <p className="text-green-300 font-bold text-2xl">
                ✨ REVEALED! Check Your Rarity
              </p>
            </div>
          )}

          {/* CTA 按钮组 */}
          <div className="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <button
              onClick={() => router.push('/mint')}
              disabled={!rarityPoolSet || totalMinted === BigInt(MAX_SUPPLY)}
              className="group relative px-8 py-4 bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white font-bold text-lg rounded-xl transform hover:scale-105 transition-all duration-300 shadow-2xl hover:shadow-purple-500/50 disabled:opacity-50 disabled:cursor-not-allowed"
            >
              <span className="relative z-10">
                {totalMinted === BigInt(MAX_SUPPLY) ? '✅ Sold Out' : '🎲 Mint Blind Box'}
              </span>
              <div className="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-600 rounded-xl blur-lg opacity-75 group-hover:opacity-100 transition-opacity"></div>
            </button>

            <button
              onClick={() => router.push('/my-nfts')}
              className="px-8 py-4 border-2 border-purple-400/50 hover:border-purple-400 text-purple-400 hover:text-white hover:bg-purple-400/10 font-bold text-lg rounded-xl transform hover:scale-105 transition-all duration-300"
            >
              🖼️ My Collection
            </button>
          </div>

          {/* 铸造进度条 */}
          <CollectionProgress
            totalMinted={totalMinted}
            maxSupply={MAX_SUPPLY}
            mintPrice={MINT_PRICE}
            isRevealed={isRevealed}
          />

          {/* 核心特性展示 */}
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-3xl mx-auto mt-12">
            <FeatureCard
              icon="🎲"
              title="Blind Box Mystery"
              description="Every mint is unrevealed until sellout"
            />
            <FeatureCard
              icon="🔒"
              title="Batch Reveal"
              description="Fair VRF-based reveal after 100/100 minted"
            />
            <FeatureCard
              icon="💎"
              title="Rarity Rewards"
              description="Legendary NFTs earn 3x staking rewards"
            />
          </div>
        </div>
      </div>
    </section>
  );
}
```

**铸造进度组件**

```tsx
function CollectionProgress({
  totalMinted,
  maxSupply,
  mintPrice,
  isRevealed
}: {
  totalMinted: bigint | undefined;
  maxSupply: number;
  mintPrice: string;
  isRevealed: boolean | undefined;
}) {
  const progress = totalMinted ? Number((totalMinted * 100n) / BigInt(maxSupply)) : 0;
  const remaining = maxSupply - Number(totalMinted || 0);

  return (
    <div className="max-w-2xl mx-auto">
      <div className="text-center mb-6">
        <h3 className="text-2xl font-bold text-white mb-2">Collection Progress</h3>
        <p className="text-gray-400">
          Limited Edition • Only {remaining} Blind Boxes Remaining
        </p>
      </div>

      {/* 进度条 */}
      <div className="relative">
        <div className="w-full bg-gray-800 rounded-full h-6 overflow-hidden">
          <div
            className="h-full bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 transition-all duration-1000 ease-out relative overflow-hidden"
            style={{ width: `${progress}%` }}
          >
            <div className="absolute inset-0 bg-white/20 animate-shimmer"></div>
          </div>
        </div>

        {/* 进度文字 */}
        <div className="flex justify-between items-center mt-4">
          <span className="text-lg font-semibold text-white">
            {totalMinted?.toString() || "0"} / {maxSupply} Minted
          </span>
          <span className="text-lg font-bold text-purple-400">
            {progress.toFixed(1)}%
          </span>
        </div>
      </div>

      {/* 铸造信息 */}
      <div className="text-center mt-6">
        <div className="inline-flex items-center space-x-4 bg-gray-800/50 rounded-lg px-6 py-3 backdrop-blur">
          <div className="flex items-center space-x-2">
            <span className="text-gray-400">Mint Price:</span>
            <span className="text-white font-bold">{mintPrice}</span>
          </div>
          <div className="text-gray-600">•</div>
          <div className="flex items-center space-x-2">
            <span className="text-gray-400">Max Per Wallet:</span>
            <span className="text-white font-bold">20 NFTs</span>
          </div>
        </div>

        {/* 揭示状态徽章 */}
        <div className="mt-4">
          {isRevealed ? (
            <span className="inline-flex items-center space-x-2 bg-green-500/20 text-green-400 px-4 py-2 rounded-full border border-green-500/50">
              <span>✅</span>
              <span className="font-semibold">Rarities Revealed</span>
            </span>
          ) : (
            <span className="inline-flex items-center space-x-2 bg-yellow-500/20 text-yellow-400 px-4 py-2 rounded-full border border-yellow-500/50">
              <span>🔒</span>
              <span className="font-semibold">Blind Box State</span>
            </span>
          )}
        </div>
      </div>
    </div>
  );
}
```

---

### 4.2 Blind Box Showcase (盲盒机制说明)

```tsx
// components/sections/BlindBoxShowcase.tsx
export function BlindBoxShowcase() {
  return (
    <section className="py-20 bg-black">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-6">
            🎁 How Blind Boxes Work
          </h2>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            Experience the thrill of mystery. All NFTs start as blind boxes with hidden rarities, revealed only after complete sellout.
          </p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 max-w-6xl mx-auto">

          {/* 盲盒状态 */}
          <div className="bg-gray-900/50 backdrop-blur rounded-2xl p-8 border border-gray-800">
            <div className="text-center mb-6">
              <span className="inline-flex items-center space-x-2 bg-yellow-500/20 text-yellow-400 px-4 py-2 rounded-full border border-yellow-500/50 mb-4">
                <span>🔒</span>
                <span className="font-semibold">Unrevealed State</span>
              </span>
              <h3 className="text-2xl font-bold text-white">Before Reveal</h3>
            </div>

            {/* 盲盒图片 */}
            <div className="mb-6 flex justify-center">
              <div className="w-64 h-64 bg-gradient-to-br from-gray-700 to-gray-900 rounded-xl flex items-center justify-center border-4 border-gray-600 relative overflow-hidden group">
                <div className="absolute inset-0 bg-gradient-to-br from-purple-600/20 to-pink-600/20 animate-pulse"></div>
                <span className="text-8xl relative z-10 group-hover:scale-110 transition-transform">🎁</span>
              </div>
            </div>

            {/* 元数据示例 */}
            <div className="bg-gray-800/50 rounded-lg p-4 font-mono text-sm">
              <pre className="text-gray-300 overflow-x-auto">
{`{
  "name": "Stakable NFT #42",
  "description": "A mysterious blind box...",
  "image": "ipfs://Qmd2S...S1cKk",
  "attributes": [
    {
      "trait_type": "Status",
      "value": "Unrevealed"
    }
  ]
}`}
              </pre>
            </div>

            <div className="mt-6 space-y-2">
              <div className="flex items-center space-x-2 text-gray-400">
                <span>❌</span>
                <span>Rarity Hidden</span>
              </div>
              <div className="flex items-center space-x-2 text-gray-400">
                <span>❌</span>
                <span>Multiplier Unknown</span>
              </div>
              <div className="flex items-center space-x-2 text-gray-400">
                <span>❌</span>
                <span>Cannot Query getRarity()</span>
              </div>
            </div>
          </div>

          {/* 揭示状态 */}
          <div className="bg-gray-900/50 backdrop-blur rounded-2xl p-8 border border-green-500/50">
            <div className="text-center mb-6">
              <span className="inline-flex items-center space-x-2 bg-green-500/20 text-green-400 px-4 py-2 rounded-full border border-green-500/50 mb-4">
                <span>✅</span>
                <span className="font-semibold">Revealed State</span>
              </span>
              <h3 className="text-2xl font-bold text-white">After Reveal</h3>
            </div>

            {/* 揭示后图片(示例:Legendary) */}
            <div className="mb-6 flex justify-center">
              <div className="w-64 h-64 bg-gradient-to-br from-yellow-500 to-orange-600 rounded-xl flex items-center justify-center border-4 border-yellow-400 relative overflow-hidden group">
                <div className="absolute inset-0 bg-gradient-to-br from-yellow-400/30 to-orange-500/30 animate-glow"></div>
                <span className="text-8xl relative z-10 group-hover:scale-110 transition-transform">🌟</span>
              </div>
            </div>

            {/* 元数据示例 */}
            <div className="bg-gray-800/50 rounded-lg p-4 font-mono text-sm">
              <pre className="text-gray-300 overflow-x-auto text-xs">
{`{
  "name": "Stakable NFT #42",
  "description": "A stakable NFT with Legendary rarity...",
  "image": "ipfs://QmZH...sjtf",
  "attributes": [
    {
      "trait_type": "Rarity",
      "value": "Legendary"
    },
    {
      "trait_type": "Reward Multiplier",
      "value": "3x"
    },
    {
      "trait_type": "Multiplier Value",
      "value": 30000
    },
    {
      "trait_type": "Status",
      "value": "Revealed"
    }
  ]
}`}
              </pre>
            </div>

            <div className="mt-6 space-y-2">
              <div className="flex items-center space-x-2 text-green-400">
                <span>✅</span>
                <span>Rarity Visible</span>
              </div>
              <div className="flex items-center space-x-2 text-green-400">
                <span>✅</span>
                <span>Multiplier Known</span>
              </div>
              <div className="flex items-center space-x-2 text-green-400">
                <span>✅</span>
                <span>Ready to Stake</span>
              </div>
            </div>
          </div>
        </div>

        {/* 揭示触发说明 */}
        <div className="mt-16 max-w-3xl mx-auto">
          <div className="bg-gradient-to-r from-purple-900/50 to-pink-900/50 backdrop-blur rounded-2xl p-8 border border-purple-500/50">
            <h3 className="text-2xl font-bold text-white mb-4 text-center">
              🎲 VRF-Based Fair Reveal
            </h3>
            <div className="space-y-4 text-gray-300">
              <p>
                <strong className="text-purple-400">When?</strong> Only after all 100 NFTs are minted (sellout)
              </p>
              <p>
                <strong className="text-purple-400">Who?</strong> Admin triggers reveal using <code className="bg-black/50 px-2 py-1 rounded">reveal()</code> function
              </p>
              <p>
                <strong className="text-purple-400">How?</strong> Random offset calculated via VRF (Verifiable Random Function)
              </p>
              <p>
                <strong className="text-purple-400">Result:</strong> All 100 NFTs reveal simultaneously with fair rarity distribution
              </p>
            </div>

            <div className="mt-6 p-4 bg-black/30 rounded-lg">
              <p className="text-sm text-gray-400 leading-relaxed">
                <span className="text-yellow-400 font-semibold">⚠️ Note:</span> Current implementation uses <code className="bg-black/50 px-1 rounded">blockhash</code> for randomness. Production deployment will integrate Chainlink VRF for cryptographically secure randomness.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

---

### 4.3 Rarity Showcase (稀有度展示)

```tsx
// components/sections/RarityShowcase.tsx
export function RarityShowcase() {
  return (
    <section className="py-20 bg-gray-900">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-6">
            💎 Rarity Distribution & Rewards
          </h2>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            Each NFT has a unique rarity that determines staking rewards. Higher rarity = Higher multiplier!
          </p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
          {rarityData.map((rarity) => (
            <RarityCard key={rarity.name} rarity={rarity} />
          ))}
        </div>

        {/* 稀有度对比表格 */}
        <div className="mt-16 max-w-4xl mx-auto">
          <div className="bg-gray-800/50 backdrop-blur rounded-2xl p-6 border border-gray-700">
            <h3 className="text-xl font-bold text-white mb-6 text-center">Rarity Comparison</h3>
            <div className="overflow-x-auto">
              <table className="w-full text-left">
                <thead>
                  <tr className="border-b border-gray-700">
                    <th className="py-3 px-4 text-gray-400">Rarity</th>
                    <th className="py-3 px-4 text-gray-400">Quantity</th>
                    <th className="py-3 px-4 text-gray-400">Distribution</th>
                    <th className="py-3 px-4 text-gray-400">Multiplier</th>
                    <th className="py-3 px-4 text-gray-400">Multiplier Value</th>
                  </tr>
                </thead>
                <tbody>
                  {rarityData.map((rarity) => (
                    <tr key={rarity.name} className="border-b border-gray-700/50 last:border-b-0 hover:bg-gray-700/30 transition-colors">
                      <td className="py-3 px-4">
                        <div className="flex items-center space-x-2">
                          <span className="text-2xl">{rarity.icon}</span>
                          <span className={`font-semibold ${rarity.textClass}`}>{rarity.name}</span>
                        </div>
                      </td>
                      <td className="py-3 px-4 text-gray-300 font-medium">{rarity.quantity}</td>
                      <td className="py-3 px-4">
                        <div className="flex items-center space-x-2">
                          <div className="flex-1 bg-gray-700 rounded-full h-2 max-w-[100px]">
                            <div
                              className={`h-full rounded-full ${rarity.bgClass}`}
                              style={{ width: `${rarity.percentage}%` }}
                            ></div>
                          </div>
                          <span className="text-gray-300 text-sm">{rarity.percentage}%</span>
                        </div>
                      </td>
                      <td className="py-3 px-4">
                        <span className={`font-bold ${rarity.textClass}`}>{rarity.multiplier}</span>
                      </td>
                      <td className="py-3 px-4">
                        <code className="bg-black/50 px-2 py-1 rounded text-sm text-gray-300">
                          {rarity.multiplierValue}
                        </code>
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>

            <div className="mt-6 p-4 bg-purple-500/10 border border-purple-500/30 rounded-lg">
              <p className="text-sm text-gray-300">
                <span className="text-purple-400 font-semibold">ℹ️ Multiplier Base:</span> All values use base 10000 (e.g., 15000 = 1.5x). This precision allows for accurate reward calculations in the staking pool contract.
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**稀有度数据定义** (基于合约实际值)

```tsx
const rarityData = [
  {
    name: 'Common',
    icon: '⚪',
    quantity: 50,
    percentage: 50,
    multiplier: '1.0x',
    multiplierValue: 10000,
    bgClass: 'bg-gray-500',
    textClass: 'text-gray-300',
    description: '50% chance - Standard rewards'
  },
  {
    name: 'Rare',
    icon: '🔵',
    quantity: 30,
    percentage: 30,
    multiplier: '1.5x',
    multiplierValue: 15000,
    bgClass: 'bg-blue-500',
    textClass: 'text-blue-400',
    description: '30% chance - Enhanced rewards'
  },
  {
    name: 'Epic',
    icon: '🟣',
    quantity: 15,
    percentage: 15,
    multiplier: '2.0x',
    multiplierValue: 20000,
    bgClass: 'bg-purple-500',
    textClass: 'text-purple-400',
    description: '15% chance - Double rewards'
  },
  {
    name: 'Legendary',
    icon: '🌟',
    quantity: 5,
    percentage: 5,
    multiplier: '3.0x',
    multiplierValue: 30000,
    bgClass: 'bg-yellow-500',
    textClass: 'text-yellow-400',
    description: '5% chance - Triple rewards!'
  }
];
```

**稀有度卡片组件**

```tsx
function RarityCard({ rarity }: { rarity: typeof rarityData[0] }) {
  return (
    <div className={`group relative bg-gradient-to-br ${rarity.bgClass} bg-opacity-10 backdrop-blur rounded-2xl p-6 border-2 ${rarity.bgClass.replace('bg-', 'border-')} hover:scale-105 transition-all duration-300`}>
      {/* 稀有度图标 */}
      <div className="text-center mb-4">
        <span className="text-6xl group-hover:scale-110 transition-transform inline-block">
          {rarity.icon}
        </span>
      </div>

      {/* 稀有度名称 */}
      <h3 className={`text-2xl font-bold ${rarity.textClass} text-center mb-4`}>
        {rarity.name}
      </h3>

      {/* 分布信息 */}
      <div className="space-y-3">
        <div className="flex justify-between items-center">
          <span className="text-gray-400 text-sm">Quantity:</span>
          <span className="text-white font-semibold">{rarity.quantity} NFTs</span>
        </div>

        <div className="flex justify-between items-center">
          <span className="text-gray-400 text-sm">Chance:</span>
          <span className="text-white font-semibold">{rarity.percentage}%</span>
        </div>

        <div className="flex justify-between items-center">
          <span className="text-gray-400 text-sm">Multiplier:</span>
          <span className={`font-bold text-lg ${rarity.textClass}`}>{rarity.multiplier}</span>
        </div>

        <div className="flex justify-between items-center">
          <span className="text-gray-400 text-sm">Base Value:</span>
          <code className="bg-black/50 px-2 py-1 rounded text-xs text-gray-300">
            {rarity.multiplierValue}
          </code>
        </div>
      </div>

      {/* 描述 */}
      <p className="mt-4 text-center text-sm text-gray-400 leading-relaxed">
        {rarity.description}
      </p>

      {/* 悬停光效 */}
      <div className={`absolute inset-0 ${rarity.bgClass} opacity-0 group-hover:opacity-20 rounded-2xl transition-opacity blur-xl`}></div>
    </div>
  );
}
```

---

### 4.4 Live Activity (实时铸造动态)

```tsx
// components/sections/LiveActivity.tsx
export function LiveActivity() {
  const [recentMints, setRecentMints] = useState<MintEvent[]>([]);

  // 监听NFTMinted事件 (注意:使用正确的事件结构)
  useScaffoldWatchContractEvent({
    contractName: "StakableNFT",
    eventName: "NFTMinted",
    onLogs: (logs) => {
      const newMints = logs.map(log => ({
        transactionHash: log.transactionHash,
        owner: log.args.to,
        startTokenId: log.args.startTokenId,  // ✅ 正确字段
        quantity: log.args.quantity,           // ✅ 正确字段
        timestamp: Date.now(),
      }));

      setRecentMints(prev => [...newMints, ...prev].slice(0, 20));
    },
  });

  return (
    <section className="py-20 bg-black">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-12">
          <div className="flex items-center justify-center space-x-3 mb-6">
            <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white">
              🔥 Live Mint Activity
            </h2>
            <div className="flex items-center space-x-2 bg-green-500/20 rounded-full px-3 py-1 border border-green-500/50">
              <div className="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
              <span className="text-green-400 text-sm font-medium">LIVE</span>
            </div>
          </div>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            Watch as collectors mint their blind boxes in real-time. Who will be the lucky one to get Legendary?
          </p>
        </div>

        <div className="max-w-5xl mx-auto">
          <div className="bg-gray-900/50 backdrop-blur rounded-2xl p-6 sm:p-8 border border-gray-800">

            {/* Mint动态列表 */}
            <div className="space-y-3 max-h-96 overflow-y-auto" id="mint-feed">
              {recentMints.length > 0 ? (
                recentMints.map((mint, index) => (
                  <MintFeedItem key={`${mint.transactionHash}-${index}`} mint={mint} index={index} />
                ))
              ) : (
                <div className="text-center py-16">
                  <div className="text-6xl mb-4">⏳</div>
                  <h3 className="text-xl font-semibold text-white mb-2">Waiting for First Mint</h3>
                  <p className="text-gray-400">Be the first to mint a mystery blind box!</p>
                  <button
                    onClick={() => window.location.href = '/mint'}
                    className="mt-6 px-6 py-3 bg-gradient-to-r from-purple-600 to-pink-600 text-white font-bold rounded-xl hover:scale-105 transition-transform"
                  >
                    🎲 Be First to Mint
                  </button>
                </div>
              )}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}
```

**Mint动态条目组件**

```tsx
interface MintEvent {
  transactionHash: string;
  owner: string;
  startTokenId: bigint;
  quantity: bigint;
  timestamp: number;
}

function MintFeedItem({ mint, index }: { mint: MintEvent; index: number }) {
  const timeAgo = useTimeAgo(mint.timestamp);
  const quantity = Number(mint.quantity);
  const tokenIds = Array.from(
    { length: quantity },
    (_, i) => Number(mint.startTokenId) + i
  );

  return (
    <div
      className="flex items-center justify-between p-4 bg-gray-800/50 hover:bg-gray-800/70 rounded-xl border border-gray-700/50 transition-all animate-slide-in"
      style={{ animationDelay: `${index * 50}ms` }}
    >
      <div className="flex items-center space-x-4 flex-1">
        {/* 盲盒图标 */}
        <div className="w-12 h-12 bg-gradient-to-br from-purple-600 to-pink-600 rounded-lg flex items-center justify-center flex-shrink-0">
          <span className="text-2xl">🎁</span>
        </div>

        {/* Mint信息 */}
        <div className="flex-1 min-w-0">
          <div className="flex items-center space-x-2 mb-1">
            <span className="text-white font-semibold truncate">
              {mint.owner.slice(0, 6)}...{mint.owner.slice(-4)}
            </span>
            <span className="text-gray-400 text-sm">minted</span>
            <span className="text-purple-400 font-bold">
              {quantity} Blind Box{quantity > 1 ? 'es' : ''}
            </span>
          </div>

          <div className="flex items-center space-x-2 text-xs text-gray-500">
            <span>Token ID: {tokenIds.join(', ')}</span>
            <span>•</span>
            <span>{timeAgo}</span>
          </div>
        </div>
      </div>

      {/* Transaction链接 */}
      <a
        href={`https://etherscan.io/tx/${mint.transactionHash}`}
        target="_blank"
        rel="noopener noreferrer"
        className="ml-4 text-gray-400 hover:text-purple-400 transition-colors flex-shrink-0"
        title="View on Etherscan"
      >
        <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
        </svg>
      </a>
    </div>
  );
}
```

---

### 4.5 Reveal Process (揭示流程说明)

```tsx
// components/sections/RevealProcess.tsx
export function RevealProcess() {
  const steps = [
    {
      number: 1,
      icon: '🔗',
      title: 'Connect Wallet',
      description: 'Connect your Web3 wallet (MetaMask, WalletConnect, etc.)',
      details: 'Supports all major Ethereum wallets via RainbowKit',
      status: 'User Action'
    },
    {
      number: 2,
      icon: '🎁',
      title: 'Mint Blind Box',
      description: 'Pay 1 ETH to mint your mystery NFT (max 20 per wallet)',
      details: 'Receive unrevealed NFT with blind box metadata',
      status: 'User Action'
    },
    {
      number: 3,
      icon: '⏳',
      title: 'Wait for Sellout',
      description: 'All 100 NFTs must be minted before reveal can trigger',
      details: 'Track progress in real-time on homepage',
      status: 'Community Progress'
    },
    {
      number: 4,
      icon: '🎲',
      title: 'Admin Triggers Reveal',
      description: 'Project admin calls reveal() function with VRF randomness',
      details: 'Random offset ensures fair rarity distribution',
      status: 'Admin Action'
    },
    {
      number: 5,
      icon: '✨',
      title: 'Batch Reveal',
      description: 'All 100 NFTs reveal simultaneously with assigned rarities',
      details: 'Metadata updates instantly via on-chain generation',
      status: 'Automated'
    },
    {
      number: 6,
      icon: '💎',
      title: 'Stake & Earn',
      description: 'Transfer revealed NFT to staking pool contract to earn RWRD tokens',
      details: 'Rewards based on your rarity multiplier (1x-3x)',
      status: 'User Action'
    }
  ];

  return (
    <section className="py-20 bg-gradient-to-br from-purple-900/20 to-blue-900/20">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-6">
            🎯 Complete User Journey
          </h2>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            From minting to staking in 6 transparent steps. Understand the full process before you begin.
          </p>
        </div>

        {/* 桌面端时间线布局 */}
        <div className="hidden lg:block max-w-6xl mx-auto">
          <div className="relative">
            {/* 连接线 */}
            <div className="absolute top-1/2 left-0 right-0 h-1 bg-gradient-to-r from-purple-600 via-pink-600 to-blue-600 -translate-y-1/2"></div>

            {/* 步骤卡片 */}
            <div className="grid grid-cols-6 gap-4 relative z-10">
              {steps.map((step) => (
                <StepCard key={step.number} step={step} layout="vertical" />
              ))}
            </div>
          </div>
        </div>

        {/* 移动端垂直布局 */}
        <div className="lg:hidden max-w-2xl mx-auto space-y-6">
          {steps.map((step) => (
            <StepCard key={step.number} step={step} layout="horizontal" />
          ))}
        </div>

        {/* VRF公平性说明 */}
        <div className="mt-16 max-w-4xl mx-auto">
          <div className="bg-gradient-to-r from-indigo-900/50 to-purple-900/50 backdrop-blur rounded-2xl p-8 border border-indigo-500/50">
            <h3 className="text-2xl font-bold text-white mb-6 text-center flex items-center justify-center space-x-3">
              <span>🔐</span>
              <span>Provably Fair Randomness</span>
            </h3>

            <div className="grid md:grid-cols-2 gap-6">
              <div className="bg-black/30 rounded-xl p-6">
                <h4 className="text-purple-400 font-semibold mb-3">How VRF Works</h4>
                <ol className="space-y-2 text-sm text-gray-300">
                  <li>1. Admin calls <code className="bg-black/50 px-1 rounded">reveal()</code> after sellout</li>
                  <li>2. Random number generated via VRF oracle</li>
                  <li>3. Offset calculated: <code className="bg-black/50 px-1 rounded">offset = randomNum % 100</code></li>
                  <li>4. Each tokenId maps to: <code className="bg-black/50 px-1 rounded">rarityPool[(tokenId + offset) % 100]</code></li>
                  <li>5. All rarities assigned in single transaction</li>
                </ol>
              </div>

              <div className="bg-black/30 rounded-xl p-6">
                <h4 className="text-pink-400 font-semibold mb-3">Why It's Fair</h4>
                <ul className="space-y-2 text-sm text-gray-300">
                  <li>✅ Rarity pool shuffled off-chain before deployment</li>
                  <li>✅ Random offset prevents prediction</li>
                  <li>✅ All 100 NFTs revealed simultaneously</li>
                  <li>✅ No individual advantage based on mint order</li>
                  <li>✅ Distribution locked to 50/30/15/5%</li>
                </ul>
              </div>
            </div>

            <div className="mt-6 p-4 bg-yellow-500/10 border border-yellow-500/30 rounded-lg">
              <p className="text-sm text-gray-300 leading-relaxed">
                <span className="text-yellow-400 font-semibold">⚠️ Current Implementation:</span> Using <code className="bg-black/50 px-1 rounded">keccak256(block.timestamp, block.prevrandao, msg.sender, totalMinted)</code> for demo. Production will integrate <strong>Chainlink VRF</strong> for cryptographic randomness.
              </p>
            </div>
          </div>
        </div>

        {/* CTA */}
        <div className="text-center mt-12">
          <button
            onClick={() => window.location.href = '/mint'}
            className="px-8 py-4 bg-gradient-to-r from-purple-600 to-pink-600 hover:from-purple-700 hover:to-pink-700 text-white font-bold text-lg rounded-xl transform hover:scale-105 transition-all duration-300 shadow-xl"
          >
            🚀 Start Minting Now
          </button>
          <p className="text-gray-400 mt-4 text-sm">
            New to Web3? <a href="/guide" className="text-purple-400 hover:text-purple-300 underline">Read our beginner's guide</a>
          </p>
        </div>
      </div>
    </section>
  );
}
```

**步骤卡片组件**

```tsx
function StepCard({
  step,
  layout
}: {
  step: typeof steps[0];
  layout: 'vertical' | 'horizontal'
}) {
  if (layout === 'vertical') {
    return (
      <div className="relative">
        {/* 步骤圆点 */}
        <div className="w-12 h-12 bg-gradient-to-br from-purple-600 to-pink-600 rounded-full flex items-center justify-center mx-auto mb-4 border-4 border-gray-900 relative z-20">
          <span className="text-white font-bold">{step.number}</span>
        </div>

        {/* 卡片内容 */}
        <div className="bg-gray-800/50 backdrop-blur rounded-xl p-4 border border-gray-700 hover:border-purple-500/50 transition-all">
          <div className="text-center mb-2">
            <span className="text-3xl">{step.icon}</span>
          </div>
          <h3 className="text-white font-bold text-sm mb-2 text-center">{step.title}</h3>
          <p className="text-gray-400 text-xs text-center leading-relaxed">{step.description}</p>
          <div className="mt-3 pt-3 border-t border-gray-700">
            <span className="text-xs text-purple-400 block text-center">{step.status}</span>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="flex items-start space-x-4 bg-gray-800/50 backdrop-blur rounded-xl p-6 border border-gray-700">
      <div className="flex-shrink-0">
        <div className="w-12 h-12 bg-gradient-to-br from-purple-600 to-pink-600 rounded-full flex items-center justify-center">
          <span className="text-white font-bold">{step.number}</span>
        </div>
      </div>
      <div className="flex-1">
        <div className="flex items-center space-x-2 mb-2">
          <span className="text-2xl">{step.icon}</span>
          <h3 className="text-white font-bold">{step.title}</h3>
        </div>
        <p className="text-gray-300 text-sm mb-2">{step.description}</p>
        <p className="text-gray-500 text-xs mb-2">{step.details}</p>
        <span className="inline-block text-xs bg-purple-500/20 text-purple-400 px-2 py-1 rounded">
          {step.status}
        </span>
      </div>
    </div>
  );
}
```

---

### 4.6 Staking Architecture (质押架构说明)

```tsx
// components/sections/StakingArchitecture.tsx
export function StakingArchitecture() {
  return (
    <section className="py-20 bg-gray-900">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-16">
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-6">
            🏗️ Multi-Contract Architecture
          </h2>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            Our system uses separate contracts for NFTs, staking, and rewards. This modular design ensures security and upgradeability.
          </p>
        </div>

        {/* 架构图 */}
        <div className="max-w-5xl mx-auto mb-16">
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6">

            {/* Contract 1: StakableNFT */}
            <div className="bg-gradient-to-br from-purple-900/50 to-purple-800/50 backdrop-blur rounded-2xl p-6 border-2 border-purple-500/50">
              <div className="text-center mb-4">
                <div className="w-16 h-16 bg-purple-600 rounded-xl flex items-center justify-center mx-auto mb-3">
                  <span className="text-3xl">🎨</span>
                </div>
                <h3 className="text-xl font-bold text-white">StakableNFT</h3>
                <p className="text-purple-300 text-sm mt-1">ERC721 Contract</p>
              </div>

              <div className="space-y-2 text-sm">
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Mint blind box NFTs</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Store rarity metadata</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Provide reward multipliers</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Trigger VRF reveal</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-red-400 mt-0.5">✗</span>
                  <span className="text-gray-500">No staking logic</span>
                </div>
              </div>
            </div>

            {/* Arrow */}
            <div className="hidden md:flex items-center justify-center">
              <div className="text-center">
                <div className="text-4xl text-purple-400 mb-2">→</div>
                <p className="text-xs text-gray-500">Transfer NFT</p>
              </div>
            </div>

            {/* Contract 2: NFTStakingPool */}
            <div className="bg-gradient-to-br from-pink-900/50 to-pink-800/50 backdrop-blur rounded-2xl p-6 border-2 border-pink-500/50">
              <div className="text-center mb-4">
                <div className="w-16 h-16 bg-pink-600 rounded-xl flex items-center justify-center mx-auto mb-3">
                  <span className="text-3xl">🔒</span>
                </div>
                <h3 className="text-xl font-bold text-white">NFTStakingPool</h3>
                <p className="text-pink-300 text-sm mt-1">Staking Contract</p>
              </div>

              <div className="space-y-2 text-sm">
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Accept NFT deposits</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Calculate time-based rewards</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Apply rarity multipliers</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Distribute RWRD tokens</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Handle withdrawals</span>
                </div>
              </div>
            </div>

          </div>

          {/* Arrow down */}
          <div className="flex justify-center my-6">
            <div className="text-center">
              <div className="text-4xl text-pink-400">↓</div>
              <p className="text-xs text-gray-500 mt-1">Earn Rewards</p>
            </div>
          </div>

          {/* Contract 3: RewardToken */}
          <div className="max-w-md mx-auto">
            <div className="bg-gradient-to-br from-blue-900/50 to-blue-800/50 backdrop-blur rounded-2xl p-6 border-2 border-blue-500/50">
              <div className="text-center mb-4">
                <div className="w-16 h-16 bg-blue-600 rounded-xl flex items-center justify-center mx-auto mb-3">
                  <span className="text-3xl">💰</span>
                </div>
                <h3 className="text-xl font-bold text-white">RewardToken (RWRD)</h3>
                <p className="text-blue-300 text-sm mt-1">ERC20 Contract</p>
              </div>

              <div className="space-y-2 text-sm">
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Standard ERC20 token</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Minted to staking pool</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Distributed to stakers</span>
                </div>
                <div className="flex items-start space-x-2">
                  <span className="text-green-400 mt-0.5">✓</span>
                  <span className="text-gray-300">Tradeable on DEXs</span>
                </div>
              </div>
            </div>
          </div>
        </div>

        {/* 关键说明 */}
        <div className="max-w-4xl mx-auto grid md:grid-cols-2 gap-6">
          <div className="bg-gray-800/50 backdrop-blur rounded-xl p-6 border border-gray-700">
            <h3 className="text-white font-bold mb-4 flex items-center space-x-2">
              <span>💡</span>
              <span>Why Separate Contracts?</span>
            </h3>
            <ul className="space-y-2 text-sm text-gray-300">
              <li className="flex items-start space-x-2">
                <span className="text-purple-400 mt-0.5">•</span>
                <span><strong>Security:</strong> Isolated risk - exploit in one doesn't affect others</span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-purple-400 mt-0.5">•</span>
                <span><strong>Upgradability:</strong> Can deploy new staking logic without touching NFTs</span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-purple-400 mt-0.5">•</span>
                <span><strong>Flexibility:</strong> Same NFT can work with multiple staking pools</span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-purple-400 mt-0.5">•</span>
                <span><strong>Gas Efficiency:</strong> Smaller contracts = cheaper deployments</span>
              </li>
            </ul>
          </div>

          <div className="bg-gray-800/50 backdrop-blur rounded-xl p-6 border border-gray-700">
            <h3 className="text-white font-bold mb-4 flex items-center space-x-2">
              <span>⚙️</span>
              <span>How They Interact</span>
            </h3>
            <ol className="space-y-2 text-sm text-gray-300">
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">1.</span>
                <span>User mints NFT from <code className="bg-black/50 px-1 rounded text-xs">StakableNFT</code></span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">2.</span>
                <span>User approves <code className="bg-black/50 px-1 rounded text-xs">NFTStakingPool</code> to transfer NFT</span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">3.</span>
                <span>Pool contract calls <code className="bg-black/50 px-1 rounded text-xs">getRewardMultiplier(tokenId)</code></span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">4.</span>
                <span>Pool transfers NFT to itself via <code className="bg-black/50 px-1 rounded text-xs">safeTransferFrom()</code></span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">5.</span>
                <span>Pool calculates rewards using multiplier</span>
              </li>
              <li className="flex items-start space-x-2">
                <span className="text-pink-400 font-semibold">6.</span>
                <span>Pool transfers RWRD tokens to user</span>
              </li>
            </ol>
          </div>
        </div>

        {/* CTA */}
        <div className="text-center mt-12">
          <a
            href="/stake"
            className="inline-block px-8 py-4 bg-gradient-to-r from-pink-600 to-purple-600 hover:from-pink-700 hover:to-purple-700 text-white font-bold text-lg rounded-xl transform hover:scale-105 transition-all duration-300 shadow-xl"
          >
            🔒 Explore Staking Pool
          </a>
        </div>
      </div>
    </section>
  );
}
```

---

### 4.7 Stats Dashboard (数据仪表板)

```tsx
// components/sections/StatsDashboard.tsx
export function StatsDashboard() {
  // 读取链上数据
  const { data: totalMinted } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "totalMinted",
  });

  const { data: isRevealed } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "isRevealed",
  });

  const { data: rarityPoolSet } = useScaffoldReadContract({
    contractName: "StakableNFT",
    functionName: "rarityPoolSet",
  });

  const stats = [
    {
      label: 'Total Minted',
      value: `${totalMinted?.toString() || '0'} / 100`,
      change: totalMinted ? '+' + Number((totalMinted * 100n) / 100n) + '%' : '0%',
      icon: '🎨',
      color: 'purple'
    },
    {
      label: 'Rarity Pool',
      value: rarityPoolSet ? 'Active' : 'Not Set',
      change: rarityPoolSet ? 'ready' : 'pending',
      icon: '🎲',
      color: 'blue'
    },
    {
      label: 'Reveal Status',
      value: isRevealed ? 'Revealed' : 'Unrevealed',
      change: isRevealed ? 'complete' : 'pending',
      icon: '✨',
      color: 'green'
    },
    {
      label: 'Mint Price',
      value: '1 ETH',
      change: 'fixed',
      icon: '💰',
      color: 'yellow'
    },
    {
      label: 'Max Per Wallet',
      value: '20 NFTs',
      change: 'limit',
      icon: '🔒',
      color: 'red'
    },
    {
      label: 'Total Supply',
      value: '100',
      change: 'fixed',
      icon: '📦',
      color: 'orange'
    }
  ];

  return (
    <section className="py-20 bg-gray-900">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <div className="text-center mb-12">
          <h2 className="text-3xl sm:text-4xl md:text-5xl font-bold text-white mb-6">
            📊 Collection Statistics
          </h2>
          <p className="text-xl text-gray-400 max-w-3xl mx-auto">
            Real-time on-chain data. All numbers are verifiable and transparent.
          </p>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-6">
          {stats.map((stat) => (
            <div
              key={stat.label}
              className="bg-gray-800/50 backdrop-blur rounded-2xl p-6 border border-gray-700 hover:border-gray-600 transition-all duration-300 group"
            >
              <div className="flex items-center justify-between mb-4">
                <span className="text-3xl group-hover:scale-110 transition-transform">{stat.icon}</span>
                <div className={`text-xs font-medium px-2 py-1 rounded-full ${
                  stat.change.startsWith('+') ? 'bg-green-500/20 text-green-400' :
                  stat.change === 'complete' || stat.change === 'ready' ? 'bg-green-500/20 text-green-400' :
                  stat.change === 'pending' ? 'bg-yellow-500/20 text-yellow-400' :
                  'bg-gray-500/20 text-gray-400'
                }`}>
                  {stat.change}
                </div>
              </div>

              <div className="text-2xl font-bold text-white mb-1">{stat.value}</div>
              <div className="text-sm text-gray-400">{stat.label}</div>
            </div>
          ))}
        </div>

        {/* 链接到详细统计页面 */}
        <div className="text-center mt-12">
          <a
            href="/stats"
            className="inline-block px-6 py-3 bg-gray-800 hover:bg-gray-700 text-white rounded-xl font-medium transition-colors"
          >
            📈 View Detailed Statistics
          </a>
        </div>
      </div>
    </section>
  );
}
```

---

## 5. Footer 底部设计

**已实现**: 参考 `packages/nextjs/components/Footer.tsx`

Footer组件已在之前的对话中完成,包含:
- ✅ 品牌信息和Logo
- ✅ 社交媒体链接(Twitter, Discord, GitHub, Telegram)
- ✅ 快速链接(Mint, My Collection, Staking, Statistics)
- ✅ 项目信息(About, Documentation, FAQ)
- ✅ 技术栈展示(Ethereum, Solidity, Hardhat, Next.js, RainbowKit, Wagmi, Ponder, TailwindCSS)
- ✅ 版权信息

无需重复设计,直接使用现有组件即可。

---

## 6. 响应式设计

### 6.1 断点系统

```css
/* TailwindCSS断点 */
sm: 640px   /* 小屏幕 (手机横屏) */
md: 768px   /* 平板 */
lg: 1024px  /* 小型桌面 */
xl: 1280px  /* 桌面 */
2xl: 1536px /* 大屏桌面 */
```

### 6.2 响应式布局策略

```tsx
// 首页主布局
export default function HomePage() {
  return (
    <div className="min-h-screen bg-gray-950">
      {/* 固定头部 - pt-16为内容留出空间 */}
      <Header />

      {/* 主要内容 */}
      <main className="pt-16">

        {/* Hero Section - 全屏高度 */}
        <section className="min-h-screen">
          <HeroSection />
        </section>

        {/* Blind Box Showcase */}
        <BlindBoxShowcase />

        {/* 稀有度展示 - 移动端单列,桌面端4列 */}
        <RarityShowcase />

        {/* Live Activity */}
        <LiveActivity />

        {/* Reveal Process */}
        <RevealProcess />

        {/* Staking Architecture */}
        <StakingArchitecture />

        {/* 统计数据 - 移动端2列,桌面端6列 */}
        <StatsDashboard />
      </main>

      {/* Footer */}
      <Footer />
    </div>
  );
}
```

---

## 7. 交互效果

### 7.1 动画系统

```css
/* tailwind.config.ts - 自定义动画 */
module.exports = {
  theme: {
    extend: {
      animation: {
        'float': 'float 3s ease-in-out infinite',
        'float-slow': 'float-slow 4s ease-in-out infinite',
        'gradient': 'gradient 3s ease-in-out infinite',
        'shimmer': 'shimmer 2s ease-in-out infinite',
        'glow': 'glow 2s ease-in-out infinite',
        'slide-in': 'slideIn 0.3s ease-out',
      },
      keyframes: {
        float: {
          '0%, 100%': { transform: 'translateY(0px)' },
          '50%': { transform: 'translateY(-20px)' },
        },
        'float-slow': {
          '0%, 100%': { transform: 'translateY(0px) rotate(0deg)' },
          '50%': { transform: 'translateY(-15px) rotate(5deg)' },
        },
        gradient: {
          '0%, 100%': { backgroundPosition: '0% 50%' },
          '50%': { backgroundPosition: '100% 50%' },
        },
        shimmer: {
          '0%': { transform: 'translateX(-100%)' },
          '100%': { transform: 'translateX(100%)' },
        },
        glow: {
          '0%, 100%': { boxShadow: '0 0 20px rgba(139, 92, 246, 0.5)' },
          '50%': { boxShadow: '0 0 40px rgba(139, 92, 246, 0.8)' },
        },
        slideIn: {
          '0%': { transform: 'translateX(-20px)', opacity: 0 },
          '100%': { transform: 'translateX(0)', opacity: 1 },
        },
      },
    },
  },
}
```

### 7.2 交互状态

```tsx
// 按钮悬停效果
<button className="group relative px-8 py-4 bg-gradient-to-r from-purple-600 to-pink-600 rounded-xl overflow-hidden transform hover:scale-105 transition-all duration-300">
  <span className="relative z-10 text-white font-bold">Mint Now</span>
  <div className="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-600 blur-lg opacity-0 group-hover:opacity-75 transition-opacity"></div>
</button>

// 卡片悬停效果
<div className="group bg-gray-800/50 backdrop-blur rounded-xl p-6 border border-gray-700 hover:border-purple-500/50 hover:bg-gray-800/70 transform hover:scale-105 hover:shadow-xl hover:shadow-purple-500/20 transition-all duration-300">
  <h3 className="text-white font-bold group-hover:text-purple-400 transition-colors">Card Title</h3>
</div>

// 加载状态
<div className="animate-pulse space-y-4">
  <div className="h-4 bg-gray-700 rounded w-3/4"></div>
  <div className="h-4 bg-gray-700 rounded w-1/2"></div>
</div>

// 禁用状态
<button
  disabled={!rarityPoolSet}
  className="px-6 py-3 bg-purple-600 text-white rounded-xl disabled:opacity-50 disabled:cursor-not-allowed disabled:hover:scale-100"
>
  Mint
</button>
```

---

## 8. 技术实现

### 8.1 页面组件结构

```tsx
// app/page.tsx
"use client";

import { HeroSection } from "~~/components/sections/HeroSection";
import { BlindBoxShowcase } from "~~/components/sections/BlindBoxShowcase";
import { RarityShowcase } from "~~/components/sections/RarityShowcase";
import { LiveActivity } from "~~/components/sections/LiveActivity";
import { RevealProcess } from "~~/components/sections/RevealProcess";
import { StakingArchitecture } from "~~/components/sections/StakingArchitecture";
import { StatsDashboard } from "~~/components/sections/StatsDashboard";

export default function HomePage() {
  return (
    <>
      <HeroSection />
      <BlindBoxShowcase />
      <RarityShowcase />
      <LiveActivity />
      <RevealProcess />
      <StakingArchitecture />
      <StatsDashboard />
    </>
  );
}
```

### 8.2 关键Hooks使用

```tsx
// 读取合约状态 (正确的函数名)
const { data: totalMinted } = useScaffoldReadContract({
  contractName: "StakableNFT",
  functionName: "totalMinted",  // ✅ 不是 totalSupply
});

const { data: isRevealed } = useScaffoldReadContract({
  contractName: "StakableNFT",
  functionName: "isRevealed",
});

const { data: rarityPoolSet } = useScaffoldReadContract({
  contractName: "StakableNFT",
  functionName: "rarityPoolSet",
});

// 查询稀有度 (需先检查isRevealed)
const { data: rarity } = useScaffoldReadContract({
  contractName: "StakableNFT",
  functionName: "getRarity",
  args: [tokenId],
  enabled: isRevealed === true,  // ⚠️ 重要:只有揭示后才能查询
});

// 监听铸造事件 (正确的事件结构)
useScaffoldWatchContractEvent({
  contractName: "StakableNFT",
  eventName: "NFTMinted",
  onLogs: (logs) => {
    logs.forEach(log => {
      const { to, startTokenId, quantity } = log.args;  // ✅ 正确字段
      // 注意: 没有 rarity 字段 (NFT未揭示)
    });
  },
});
```

### 8.3 样式配置

```css
/* app/globals.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

/* CSS变量 */
:root {
  --rarity-common: #9CA3AF;
  --rarity-rare: #3B82F6;
  --rarity-epic: #A855F7;
  --rarity-legendary: #F59E0B;

  --gradient-primary: linear-gradient(135deg, #8B5CF6 0%, #EC4899 100%);
  --gradient-hero: linear-gradient(135deg, #1e1b4b 0%, #312e81 50%, #1e3a8a 100%);
}

/* 基础样式 */
@layer base {
  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: 'Inter', sans-serif;
    background-color: #030712;
    color: #f9fafb;
  }

  /* 自定义滚动条 */
  ::-webkit-scrollbar {
    width: 8px;
  }

  ::-webkit-scrollbar-track {
    background: rgba(0, 0, 0, 0.1);
  }

  ::-webkit-scrollbar-thumb {
    background: rgba(139, 92, 246, 0.5);
    border-radius: 4px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: rgba(139, 92, 246, 0.7);
  }
}

/* 组件样式 */
@layer components {
  .btn {
    @apply inline-flex items-center justify-center px-6 py-3 rounded-xl font-semibold transition-all duration-200 transform;
  }

  .btn-primary {
    @apply bg-gradient-to-r from-purple-600 to-pink-600 text-white hover:from-purple-700 hover:to-pink-700 hover:scale-105 hover:shadow-xl;
  }

  .btn-secondary {
    @apply bg-gray-700 text-gray-300 hover:bg-gray-600 hover:text-white;
  }

  .glass {
    @apply bg-gray-800/50 backdrop-blur-md border border-gray-700;
  }

  .text-gradient {
    @apply bg-gradient-to-r from-purple-400 to-pink-400 bg-clip-text text-transparent;
  }
}
```

---

## 📝 实现检查清单

### 合约数据准确性
- [x] 总供应量: 100 (不是10,000)
- [x] Mint价格: 1 ETH (不是0.01 ETH)
- [x] 每钱包限额: 20 NFTs
- [x] 稀有度分布: 50/30/15/5%
- [x] 奖励倍率: 1x/1.5x/2x/3x (10000/15000/20000/30000)

### 揭示机制
- [x] 盲盒状态说明 (铸造时不可见稀有度)
- [x] 批量揭示流程 (需等待售罄)
- [x] Admin触发机制
- [x] VRF随机性说明
- [x] 揭示前后状态对比

### 用户旅程
- [x] 6步完整流程 (不是4步)
- [x] 包含"等待售罄"步骤
- [x] 包含"Admin触发揭示"步骤
- [x] 明确质押是独立合约

### 技术实现
- [x] 使用 `totalMinted` (不是totalSupply)
- [x] 使用 `isRevealed` 状态检查
- [x] 使用 `rarityPoolSet` 状态检查
- [x] 事件结构: startTokenId + quantity
- [x] getRarity()前检查isRevealed
- [x] 正确的IPFS CID引用

### UI组件
- [x] 盲盒vs揭示状态对比
- [x] 揭示状态指示器
- [x] Rarity Pool设置警告
- [x] 售罄提示
- [x] Admin面板说明
- [x] 多合约架构图

---

## 🎯 总结

本设计文档v2.0完全基于**StakableNFT.sol合约实际实现**,确保所有数据、流程、技术细节与链上代码100%一致。

### 主要更新:
1. ✅ **修正核心数据**: 供应量100, 价格1 ETH, 限额20
2. ✅ **完整揭示流程**: 盲盒→售罄→Admin触发→批量揭示
3. ✅ **分离质押架构**: 明确NFT合约vs质押池合约职责
4. ✅ **准确函数调用**: totalMinted, isRevealed, rarityPoolSet
5. ✅ **正确事件结构**: startTokenId + quantity (无rarity)
6. ✅ **VRF公平性**: 随机偏移量机制说明

### 技术栈:
- **前端**: Next.js 15 + App Router + TailwindCSS
- **Web3**: RainbowKit + Wagmi + Viem
- **合约**: Solidity + Hardhat + OpenZeppelin
- **索引**: Ponder + GraphQL
- **存储**: IPFS (链上元数据生成)

现在可以基于此设计开始前端开发,所有参数与合约完全匹配! 🚀
